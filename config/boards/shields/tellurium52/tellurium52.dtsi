/* chosen ノードを以下のように書き換えてください */
    chosen {
        zmk,kscan = &kscan0;
        zmk,matrix-transform = &default_transform; /* 一時的にこれを復活させます */
        /* zmk,physical-layout は一時的に不要になるため削除またはコメントアウト */
    };
